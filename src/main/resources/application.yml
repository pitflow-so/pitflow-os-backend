spring:
  application:
    name: pitflow-os-backend

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:pitflow_os}
    username: ${DB_USERNAME:pitflow}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    #show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
    enabled: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:pitflow.notifications@gmail.com}
    password: ${MAIL_PASSWORD}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

server:
  servlet:
    context-path: /operation
  forward-headers-strategy: framework

logging:
  level:
    org:
      liquibase: INFO

api:
  security:
    token:
      secret: ${JWT_SECRET}
      expiration-hours: 3
  url: ${API_URL:http://localhost:8080}

services:
  registry:
    base-url: ${REGISTRY_SERVICE_URL:http://localhost:8081}
  inventory:
    base-url: ${INVENTORY_SERVICE_URL:http://localhost:8082}

management:
  endpoints:
    web:
      exposure:
        include: health,metrics

  endpoint:
    health:
      show-details: never

  health:
    mail:
      enabled: false

  tracing:
    enabled: true

  metrics:
    tags:
      application: ${spring.application.name}
      service: pitflow-os-backend

  datadog:
    metrics:
      export:
        enabled: ${DATADOG_ENABLED:false}
        api-key: ${DATADOG_API_KEY}
        uri: https://api.datadoghq.com
        step: 30s

mock:
  send-message: ${MOCK_MESSAGE:true}

outbox:
  publisher:
    enabled: ${OUTBOX_PUBLISHER_ENABLED:true}
    delay-ms: ${OUTBOX_PUBLISHER_DELAY_MS:5000}
    batch-size: ${OUTBOX_PUBLISHER_BATCH_SIZE:10}
    lease-seconds: ${OUTBOX_PUBLISHER_LEASE_SECONDS:60}
    max-backoff-seconds: ${OUTBOX_PUBLISHER_MAX_BACKOFF_SECONDS:300}
