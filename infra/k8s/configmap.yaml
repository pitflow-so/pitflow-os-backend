apiVersion: v1
kind: ConfigMap
metadata:
  name: pitflow-config
  labels:
    app: pitflow-backend
data:
  DB_HOST: "${DB_HOST}"
  DB_PORT: "${DB_PORT}"
  DB_NAME: "${DB_NAME}"
  DB_USER: "${DB_USERNAME}"
  API_PUBLIC_URL: "${API_PUBLIC_URL}"
  MOCK_MESSAGE: "${MOCK_MESSAGE}"
