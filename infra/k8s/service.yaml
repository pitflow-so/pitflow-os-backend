apiVersion: v1
kind: Service
metadata:
  name: pitflow-service
spec:
  selector:
    app: pitflow-backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
